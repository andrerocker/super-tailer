require 'spec_helper'

describe Super::Tailer do
  it 'has a version number' do
    expect(Super::Tailer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
